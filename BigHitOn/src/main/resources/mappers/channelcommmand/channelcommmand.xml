<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.channelcommmand.channelcommmand">

<select id="doSelectList" parameterType="ChannelCommandVO" resultType="ChannelCommandVO"> 
	SELECT
    	ch_link,
    	com_id
	FROM channel_command;
</select>
<delete id="doDelete" parameterType="ChannelCommandVO">
	DELETE FROM channel_command
	WHERE
    	ch_link = #{chLink}
    	AND com_id = #{comId}
</delete>
<delete id="doDeleteAll" parameterType="ChannelVO">
	DELETE FROM channel_command
	WHERE
    	ch_link = #{chLink}
    	AND com_id = #{comId}
</delete>
<delete id="doDeleteAll" parameterType="ChannelCommandVO">
	DELETE FROM channel_command
	WHERE
    	ch_link = #{chLink}
    	AND com_id = #{comId}
</delete>
</mapper>